<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <style>
      body { font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Arial, sans-serif; line-height: 1.6; color: #333; }
      .container { max-width: 600px; margin: 0 auto; padding: 20px; }
      .header { background: linear-gradient(135deg, #a855f7 0%, #9333ea 100%); color: white; padding: 30px; border-radius: 12px 12px 0 0; text-align: center; }
      .content { background: white; padding: 30px; border: 1px solid #e5e7eb; border-top: none; }
      .event-card { background: #f9fafb; border-left: 4px solid #a855f7; padding: 16px; margin: 20px 0; border-radius: 8px; }
      .button { display: inline-block; padding: 14px 28px; background: #a855f7; color: white !important; text-decoration: none; border-radius: 8px; font-weight: 600; margin: 20px 0; }
      .permissions { background: #faf5ff; padding: 16px; border-radius: 8px; margin: 20px 0; }
      .permissions ul { margin: 8px 0; padding-left: 20px; }
      .footer { text-align: center; padding: 20px; color: #6b7280; font-size: 14px; }
      .note { background: #fef3c7; border-left: 4px solid #f59e0b; padding: 12px; margin: 20px 0; border-radius: 4px; font-size: 14px; }
  </style>
</head>
<body>
<div class="container">
  <div class="header">
    <h1 style="margin: 0; font-size: 28px;">🎁 GiftWise Collaboration Invite</h1>
  </div>

  <div class="content">
    <p style="font-size: 16px; margin-top: 0;">Hi there,</p>

    <p><strong><%= @inviter.name %></strong> has invited you to collaborate on their GiftWise event:</p>

    <div class="event-card">
      <h2 style="margin: 0 0 8px 0; font-size: 20px; color: #111827;"><%= @event.event_name %></h2>
      <% if @event.event_date.present? %>
        <p style="margin: 0; color: #6b7280;">📅 <%= @event.event_date.strftime("%B %d, %Y") %></p>
      <% end %>
      <p style="margin: 8px 0 0 0; color: #6b7280;">Role: <strong><%= @invite.role == Collaborator::ROLE_CO_PLANNER ? 'Co-Planner' : 'Viewer' %></strong></p>
    </div>

    <div class="permissions">
      <p style="margin: 0 0 8px 0; font-weight: 600; color: #4b5563;">As a <%= @invite.role == Collaborator::ROLE_CO_PLANNER ? 'Co-Planner' : 'Viewer' %>, you'll be able to:</p>
      <ul style="margin: 0; color: #4b5563;">
        <% @role_permissions.each do |permission| %>
          <li><%= permission %></li>
        <% end %>
      </ul>
    </div>

    <div style="text-align: center;">
      <a href="<%= @accept_url %>" class="button">Accept Invitation</a>
    </div>

    <div class="note">
      <p style="margin: 0 0 8px 0; font-weight: 600;">📌 Important:</p>
      <ul style="margin: 0; padding-left: 20px;">
        <li>You'll need a GiftWise account to accept this invitation</li>
        <li>If you don't have an account, you'll be asked to sign up first</li>
        <li>This invitation expires in 14 days</li>
      </ul>
    </div>

    <p style="color: #6b7280; font-size: 14px; margin-top: 24px;">
      If you didn't expect this invitation, you can safely ignore this email.
    </p>
  </div>

  <div class="footer">
    <p style="margin: 0;">— The GiftWise Team</p>
    <p style="margin: 8px 0 0 0; font-size: 12px;">Making gift planning easier, together.</p>
  </div>
</div>
</body>
</html>